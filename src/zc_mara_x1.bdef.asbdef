projection implementation in class ZBP_C_MARA_X1 unique;
strict ( 2 );
use draft;
define behavior for ZC_MARA_X1 alias _mara
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}